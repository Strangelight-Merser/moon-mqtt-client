name = "Strangelight-Merser/async-tls"

version = "0.1.0"

license = "Apache-2.0"

readme = "README.md"

repository = "https://github.com/Strangelight-Merser/moon-mqtt-client"

description = "Native TLS client with PEM mTLS identity support, derived from moonbitlang/async 0.21.3"

preferred_target = "native"

source = "src"

import {
  "moonbitlang/async@0.21.3",
}
