# third_party

`async-tls` is a snapshot of the independent `Strangelight-Merser/async-tls`
module used for local workspace linking and CI until that module is published
to Mooncakes. Source hashes and patches are in `async-tls/THIRD_PARTY.md`.
